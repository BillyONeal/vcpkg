diff --git a/CMakeLists.txt b/CMakeLists.txt
index f726a0a..a4caae3 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -98,6 +98,10 @@ else()
     project(blosc LANGUAGES C ASM)
 endif()
 
+if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND CMAKE_C_COMPILER_ID STREQUAL "GNU")
+    add_compile_options($<$<COMPILE_LANGUAGE:C>:-std=gnu17>)
+endif()
+
 # options
 option(BUILD_STATIC
     "Build a static version of the blosc library." ON)
